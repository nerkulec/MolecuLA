program: scripts/wandb_run.py
method: bayes
metric:
  name: val/selection_loss
  goal: minimize

project: molecula-lignin-autoregressive-rosi
name: lignin-autoregressive-stable-36h
command:
  - ${env}
  - python
  - -u
  - ${program}
  - ${args}

parameters:
  # W&B and paths. The logged-in entity/account is used automatically.
  wandb-project:
    value: molecula-lignin-autoregressive-rosi
  wandb-mode:
    value: online
  tokenizer:
    value: artifacts/lignin_retraining/unified_tokenizer.json
  shard-root:
    value: artifacts/lignin_retraining/encoded
  output-root:
    value: artifacts/lignin_retraining/sweeps/autoregressive
  log-checkpoint:
    value: true

  # Comparable 36-hour, full-dataset compute budget for every trial.
  epochs:
    value: 1000
  max-wall-clock-hours:
    value: 36
  greedy-val-samples:
    value: 32
  save-every:
    value: 0
  log-every-batches:
    value: 100
  num-workers:
    value: 4
  precision:
    value: bf16
  dataset-device:
    value: cuda
  seed:
    value: 42

  # Eight high-impact architecture and optimization settings.
  learning-rate:
    distribution: log_uniform_values
    min: 0.00003
    max: 0.0003
  max-beta:
    distribution: log_uniform_values
    min: 0.003
    max: 0.06
  hidden-size:
    values: [256, 384, 512]
  latent-size:
    values: [256, 512, 1024]
  num-slots:
    values: [4, 8, 16]
  encoder-layers:
    values: [2, 3, 4]
  decoder-layers:
    values: [1, 2, 3]
  batch-size:
    values: [32, 64, 128]

  # Fixed because they are lower-priority than the settings above.
  attn-heads:
    value: 8
  weight-decay:
    value: 0.01
  beta-warmup-epochs:
    value: 10
  lr-warmup-steps:
    value: 1000
  grad-clip:
    value: 1.0
